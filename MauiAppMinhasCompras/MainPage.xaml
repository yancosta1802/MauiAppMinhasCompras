<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiAppMinhasCompras.ViewModels"
             x:Class="MauiAppMinhasCompras.MainPage">

    <ContentPage.BindingContext>
        <local:ProdutosViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="10">
        <SearchBar Placeholder="Buscar produto..."
                   Text="{Binding TermoBusca}" />

        <CollectionView ItemsSource="{Binding ProdutosFiltrados}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="5">
                        <Label Text="{Binding Nome}" FontSize="20"/>
                        <Label Text="{Binding Preco, StringFormat='R$ {0:F2}'}" FontSize="16" TextColor="Gray"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
